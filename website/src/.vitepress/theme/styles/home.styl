/**
 * Home hero
 * --------------------------------------------------------------------------
 * No screenshot is shown in the hero — the home page is text + feature cards
 * only. Keep this file empty so it can be removed together with the import
 * in theme/index.ts when no home-specific styles are needed.
 */

